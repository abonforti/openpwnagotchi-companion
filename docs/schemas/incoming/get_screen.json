{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "incoming/get_screen.json",
  "title": "Request the e-ink frame",
  "description": "Replies with a `screen_image` message. On demand only; the client drives any auto-refresh by polling. Replies `error` with code `no_frame` when the frame file does not exist yet.",
  "type": "object",
  "properties": {
    "type": {
      "const": "get_screen"
    },
    "message_id": {
      "type": "string",
      "description": "Client-generated correlation id. When present the plugin echoes it in the reply and in the acknowledgment."
    }
  },
  "required": [
    "type"
  ],
  "additionalProperties": false
}
