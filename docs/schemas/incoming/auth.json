{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "incoming/auth.json",
  "title": "Authenticate",
  "description": "Must be the first frame when the plugin is configured with a non-empty token. The token is compared with hmac.compare_digest. A connection that has not authenticated within auth_timeout seconds is closed with code 1008.",
  "type": "object",
  "properties": {
    "type": {
      "const": "auth"
    },
    "message_id": {
      "type": "string",
      "description": "Client-generated correlation id. When present the plugin echoes it in the reply and in the acknowledgment."
    },
    "token": {
      "type": "string"
    }
  },
  "required": [
    "type",
    "token"
  ],
  "additionalProperties": false
}
