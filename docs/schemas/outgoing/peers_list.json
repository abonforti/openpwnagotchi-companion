{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "outgoing/peers_list.json",
  "title": "Peer list",
  "description": "Sent as a reply to `get_peers`. Built from agent._peers only; pwnagotchi.grid.peers() is not used because it returns a different shape.",
  "type": "object",
  "properties": {
    "type": {
      "const": "peers_list"
    },
    "data": {
      "type": "object",
      "properties": {
        "entries": {
          "type": "array",
          "items": {
            "$ref": "common.json#/$defs/Peer"
          }
        }
      },
      "required": [
        "entries"
      ],
      "additionalProperties": false
    },
    "timestamp": {
      "type": "number",
      "description": "Epoch seconds at which the plugin produced the message."
    },
    "message_id": {
      "type": "string",
      "description": "Client-generated correlation id. When present the plugin echoes it in the reply and in the acknowledgment."
    }
  },
  "required": [
    "type",
    "data",
    "timestamp"
  ],
  "additionalProperties": false
}
